# Rename this file to .env.local
# change <database name> to real one
# this bring an environment variable DATABASE_URL for using with pscale cli to push database schema to planetscale under development.
# use pscale cli: pscale connect <database name> <branch name> --port 3309

DATABASE_URL='mysql://root@127.0.0.1:3309/<database name>'
